name: Run tests of the project
on:
  pull_request:
    branches:
      - main
      - dev
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        go-version: ["1.26.3"]

    steps:
      - uses: actions/checkout@v6
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}
      - name: Download dependencies
        run: go mod download
      - name: Build project
        run: go build -v ./...
      - name: Run unit tests
        run: go test -race -v ./internal/...
      - name: Run e2e/integration tests
        run: go test -tags=integration -v -timeout=300s ./tests/integration/...
